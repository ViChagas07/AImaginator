# Grafana — datasource Prometheus provisionado como codigo (spec secao 18).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
